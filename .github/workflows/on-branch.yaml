name: On branch

on:
  push:
    branches:
      - main
      - release\/*
jobs:
  do-release-stuff:
    name: Do release stuff
    environment: on-branch
    runs-on: ubuntu-22.04
    steps:
      - name: echo secret
        run: echo ${{ secrets.DO_NOT_ECHO }}